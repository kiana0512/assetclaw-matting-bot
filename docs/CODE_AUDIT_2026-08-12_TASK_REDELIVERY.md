# 2026-08-12 任务来源与完整重发审计

## 范围

- 任务中心飞书来源姓名/头像展示
- 图片、序列帧和视频任务的完整素材重发
- ZIP 内容完整性、原会话路由、重复请求和状态隔离
- WebUI 技能代理认证与服务重载
- 原地完整重跑、重复任务折叠和取消确认交互

## 已修复

1. **来源字段泄漏内部代码**：UI 只消费 `display_name` 和 `avatar_url`，不把
   `open_id` 当作显示名；无权限时显示“飞书用户”而不是代码。
2. **历史回执导致假重发**：完整重发直接生成新包并调用飞书发送，不复用原交付
   artifact 的 `DELIVERED` 跳过逻辑。
3. **一个任务拆成多个 ZIP**：视频与序列帧均生成一个完整包。完整视频包包含
   `original_videos/frames/matte/smooth`；序列帧包包含
   `01_original_frames/02_matte/03_postprocessed`。
4. **缺少角色资料被误判不可交付**：后处理缺失时仍返回原始素材与 matte，缺失目录
   内附 README 说明。
5. **重发污染原任务状态**：使用独立 `redelivery` 状态和有界历史，不修改原任务的
   `status/stage/delivery`。
6. **重复点击与并发重发**：请求 ID、活动状态和 worker PID 防止重复提交；处理中的
   主任务禁止重发，避免生成不完整包。
7. **后台进程启动失败后永久排队**：启动异常会原子落为 `FAILED` 并记录明确错误。
8. **WebUI 认证头丢失**：Vite 代理固定从根 `.env` 注入 `SKILL_API_TOKEN`，避免 URL
   rewrite 和滚动重启导致认证头遗漏。
9. **重复信息与滚动闪烁**：空闲任务页移除重复队列；取消整行 hover 变色；重发回执
   不再额外占一行；1250px 左右窗口使用两层响应式布局，消除横向滚动和错位。
10. **上游工作流缺失节点仍进入任务**：启动前对照秋叶 `/object_info` 校验启用节点；
    缺失节点时返回明确名称并拒绝创建任务，避免 GPU 浪费和误标业务失败。
11. **用户资料轮询重复读盘**：飞书姓名头像缓存增加进程内镜像；WebUI 自动刷新不再为
    每一条任务重复读取完整 JSON 文件。
12. **并发重发竞态**：重发请求创建过程串行化，在活动状态和 PID 检查之外阻止两个
    同时到达的请求各自拉起 worker。
13. **重跑错误创建第二条任务**：完整重跑改为复用原 `run_id` 原地重置，保留来源用户、
    原始素材、列表位置和处理参数；旧状态进入 `rerun_history`，不新增父任务行。
14. **重复上传暴露两行与旧子任务假失败**：相同用户、同类型、同顺序文件名和大小的
    失败/重试记录在 UI 合并为一个业务任务；旧 ComfyUI/Cherry 子批次继续归属于历史父项，
    不再误入“独立任务”。
15. **取消仍使用浏览器原生确认框**：任务中心取消改为站内二次确认弹窗，展示用户、任务
    和当前阶段，并明确取消不会删除素材、产物或历史。

## 权限与隐私

- `contact:contact.base:readonly` 用于识别用户；`contact:user.base:readonly` 用于姓名头像。
- 应用可访问范围必须覆盖实际用户，权限变更后必须发布应用版本。
- 收件目标只来自任务持久化的 `chat_id`，UI 不接受任意目标，降低误发风险。
- 姓名头像保存到运行数据缓存，不提交 Git；代码日志不新增用户标识输出。

## 验收

- 单元测试覆盖视频完整包、视频 matte-only 包、序列帧单包和技能权限元数据。
- 生产构建通过。
- 实际 API 确认任务列表返回姓名头像，完整重发技能可由 WebUI 代理调用。
- 浏览器实际页面确认物理记录折叠为 45 个业务任务，姓名头像、完整重发和完整重跑均正常。
- 浏览器验证任务搜索（huggy 返回 3 条）、来源筛选、完整重发弹窗打开/取消、空闲队列
  去重以及连续滚动；未触发真实发送。
- 新增原地重跑回归覆盖确认门禁、任务 ID 不变、历史归档、并发幂等和视频参数保留；后端
  全量测试 `500 passed`，Vite 生产构建无告警。
- 浏览器验证旧失败父项和 `COMFY_CC646644962D` 不再单独展示，取消与原地重跑弹窗交互
  正常，控制台无 warning/error。

## ImageClip 阻塞解除

ImageClip 已更新到包含 `CodexFootRegionPasteOriginalShapeV4` 的版本；秋叶重启后运行时检查
读取到 2602 个节点类型，当前工作流缺失节点为 0，Cherry canary 通过。失败素材的等价
28 帧任务已重新进入本机 ComfyUI 抠图，旧缺节点错误仅作为历史保留。

## 后续大版本建议

- 把重发请求迁移为数据库队列，支持机器重启后的自动恢复与可观测重试策略。
- 对大 ZIP 增加内容清单 hash 与流式打包指标，便于定位磁盘和上传瓶颈。
- 为头像缓存增加过期刷新和隐私数据保留周期配置。
